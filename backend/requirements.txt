fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
pydantic>=2.9
pydantic-settings>=2.6
email-validator>=2.2
PyJWT>=2.9
bcrypt>=4.2
# Fernet, for encrypting per-workspace provider credentials at rest.
cryptography>=44
python-multipart>=0.0.12
greenlet>=3.1

# Knowledge-base ingestion: text extraction for PDF and DOCX uploads.
pypdf>=5.0
python-docx>=1.1

pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
# Test-time only: CREATE/DROP DATABASE must run outside a transaction on a
# sync connection. The application itself never uses psycopg.
psycopg[binary]>=3.2
import-linter>=2.1
